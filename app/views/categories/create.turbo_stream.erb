<%= turbo_stream.update "show_categories" do %>
	<%= render "categories", categories: Category.all %>
<% end %>

<%= turbo_stream.update "new_category" do %>
	<%= link_to "Criar uma categoria", new_category_path, data: { turbo_stream: true, turbo_frame: 'new_category' } %>
<% end %>